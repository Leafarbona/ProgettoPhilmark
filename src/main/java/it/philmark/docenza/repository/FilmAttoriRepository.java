package it.philmark.docenza.repository;

import it.philmark.docenza.models.FilmAttori;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FilmAttoriRepository extends JpaRepository<FilmAttori, Long> {
}
